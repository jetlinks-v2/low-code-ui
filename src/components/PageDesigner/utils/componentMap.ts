import { Button, Tag, Text, Form, Dropdown } from '../components/BaseComponent/index'

const componentMap = {
    'text': Text,
    'button': Button,
    'tag': Tag,
    'form': Form,
    'dropdown': Dropdown
}

export default componentMap;
