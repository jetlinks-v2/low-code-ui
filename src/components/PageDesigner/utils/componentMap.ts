import { Button, Tag, Text, Form } from '../components/BaseComponent/index'

const componentMap = {
    'text': Text,
    'button': Button,
    'tag': Tag,
    'form': Form
}

export default componentMap;
